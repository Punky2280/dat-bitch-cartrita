import { describe, it, expect } from 'vitest';

// Minimal placeholder test to keep workspace green until real tests are added.
describe('mcp-core smoke', () => {
  it('sanity: true is truthy', () => {
    expect(true).toBeTruthy();
  });
});
