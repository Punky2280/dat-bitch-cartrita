version: v1

lint:
  use:
    - STANDARD
  except:
    - PACKAGE_VERSION_SUFFIX  # Remove after all packages versioned

breaking:
  use:
    - FILE
  except:
    - EXTENSION_NO_DELETE
    - FIELD_SAME_DEFAULT
