# Root environment example (no real secrets)
NODE_ENV=development
PORT=3000
OPENAI_API_KEY=sk-REDACTED
ANTHROPIC_API_KEY=anthropic-REDACTED
DEEPGRAM_API_KEY=dg-REDACTED
HUGGINGFACE_API_KEY=hf_REDACTED
HF_ROUTER_ENABLE_PROBES=true
HF_ROUTER_PROBE_TIMEOUT_MS=3500
HF_ROUTER_MAX_CANDIDATES=6
ENCRYPTION_MASTER_KEY=change-me
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=cartrita
POSTGRES_USER=cartrita
POSTGRES_PASSWORD=change-me###############################################
# Example Environment Configuration
# Copy to .env (root) and packages/backend/.env as needed.
# Fill in real secrets locally; NEVER commit populated .env files.
###############################################

PORT=8001
NODE_ENV=development

# Postgres
POSTGRES_USER=appuser
POSTGRES_PASSWORD=change_me
POSTGRES_DB=dat-bitch-cartrita
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
DATABASE_URL=postgresql://appuser:change_me@localhost:5432/dat-bitch-cartrita

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# OpenAI
OPENAI_API_KEY=sk-REPLACE
OPENAI_MODEL=gpt-4o
OPENAI_RPM_LIMIT=60
OPENAI_TPM_LIMIT=90000
OPENAI_CONCURRENT_LIMIT=10

# LangChain / LangSmith
LANGCHAIN_TRACING_V2=true
LANGCHAIN_API_KEY=lsv2_REPLACE

# Deepgram
DEEPGRAM_API_KEY=dg_REPLACE

# Encryption
ENCRYPTION_MASTER_KEY=masterkey_change_me

# JWT
JWT_SECRET=secret-change-me

# HuggingFace (optional)
HUGGINGFACE_API_KEY=hf_REPLACE

# Misc
VECTOR_DIMENSION=1536
EMBEDDING_MODEL=text-embedding-3-large

# Do not add real keys below this line.
##############################
# Cartrita Project .env.example
# Copy to .env and fill real secrets. DO NOT COMMIT real secrets.
##############################

# --- Core Server ---
NODE_ENV=development
PORT=3001
FRONTEND_URL=http://localhost:5173

# --- Database ---
PGHOST=localhost
PGPORT=5432
PGDATABASE=cartrita
PGUSER=cartrita
PGPASSWORD=replace_me

# --- JWT / Auth ---
JWT_SECRET=replace_with_strong_secret
JWT_EXPIRY=24h

# --- OpenAI / LLM Providers (optional) ---
OPENAI_API_KEY=sk-example
DEEPGRAM_API_KEY=dg-example

# --- Observability ---
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
OTEL_SERVICE_NAME=cartrita-backend

# --- Redis (optional) ---
REDIS_URL=redis://localhost:6379

# --- Feature Flags ---
ENABLE_VECTOR_SEARCH=true
ENABLE_MCP_INTEGRATION=true

# --- Misc ---
LOG_LEVEL=info
ALLOW_ORIGIN=http://localhost:5173

##############################
# Add any additional provider keys below with clear *_API_KEY names
##############################
